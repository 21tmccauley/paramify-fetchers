# Default Skip Resources - Non-Production Resources (Terraform)
# Failed checks for matching resources are filtered out post-scan, and the
# summary is recalculated. Users add more via CHECKOV_SKIP_RESOURCES (config);
# the two lists are merged.
# Format: one resource pattern per line. Wildcards (*) supported. # = comment.

# Example entries (uncomment and customize as needed):
# aws_s3_bucket.gitlab_*
# aws_s3_bucket.nessus_scan_export
