# Placeholder. Per-fetcher test conventions are not yet defined; tests will be
# added once the framework's contract is enforced. v0.x ports are validated by
# running them end-to-end against a real tenant, not by unit tests.
